import Title from "./Title";
import Trailing from "./Trailing";
import "@/styles/Bar.scss";

const Bar = () => {
    return (
        <div className="bar">
            <Title />
            <Trailing />
        </div>
    );
};

export default Bar;
